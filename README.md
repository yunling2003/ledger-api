# ledger-api
A Go restful api manages ledger account, built with Gin, Mgo.

### Run *"yarn dev"* to start the application in debug mode.

### Run *"yarn build"* to build the application in targeting operation system linux,
### and copy output to ledger-app.
